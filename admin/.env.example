# Base URL of the Kovi API (no trailing slash). Can also be changed at runtime on the login screen.
VITE_API_URL=http://localhost:3000
# Base path when hosting under a sub-path (GitHub Pages): /kovi-running-app/
VITE_BASE_PATH=/
